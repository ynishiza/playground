ROLLBACK;
BEGIN;

DROP TABLE IF EXISTS t;

CREATE TEMPORARY TABLE t (
  id1 INT GENERATED ALWAYS AS IDENTITY,
  id2 INT GENERATED BY DEFAULT AS IDENTITY (START WITH 1 INCREMENT BY 10 MAXVALUE 300),
  sum1 INT GENERATED ALWAYS AS (id1 + id2) STORED
);

INSERT INTO t
  OVERRIDING SYSTEM VALUE VALUES (1, 1)
;
INSERT INTO t
  OVERRIDING USER VALUE VALUES (4000, 4000)
;
INSERT INTO t (id1)
  OVERRIDING SYSTEM VALUE VALUES (1)
;
INSERT INTO t DEFAULT VALUES;
INSERT INTO t DEFAULT VALUES;

INSERT INTO t (id2) VALUES (10);

INSERT INTO t (id1, id2)
  OVERRIDING SYSTEM VALUE VALUES (100, 200), (300, 400)
;
INSERT INTO t
  OVERRIDING SYSTEM VALUE VALUES (100, 200), (300, 400)
;
INSERT INTO t (id1, id2)
  OVERRIDING USER VALUE VALUES (1, 2), (3, 4)
;

INSERT INTO t DEFAULT VALUES;
INSERT INTO t DEFAULT VALUES;

SELECT * FROM t;

COMMIT;
